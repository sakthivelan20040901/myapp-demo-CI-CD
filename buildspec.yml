version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Skipping build for simple Node.js app
  post_build:
    commands:
      - echo Build completed on `date`

artifacts:
  files:
    - '**/*'
